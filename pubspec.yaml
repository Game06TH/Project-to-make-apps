name: easy_crop_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  excel: ^2.0.4        # เพิ่ม dependency สำหรับ Excel
  image_picker: ^0.8.5+3 # เพิ่ม dependency สำหรับ Image Picker

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/students_by_class_fixed.xlsx  # Path for Excel file
    - assets/mylogo.png                   # Path for logo
